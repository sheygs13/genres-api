## Installation

```shell

   # Clone the repo
   git clone https://github.com/sheygs13/rental-api.git

   # Install dependencies
   npm install

   # Rename .env.sample to .env & set secret key
   mv .env.sample .env
   JWT_KEY=<your-key>

   # Run the app
    npm start

```
