## Installation

```shell

   # Clone the repo
   git clone https://github.com/sheygs13/rental-api.git

   # Install dependencies
   npm install

   # Rename `.env.sample` to `.env` 
   mv .env.sample .env
   
   # Set secret key
   JWT_KEY=XXXXXXXXX

   # Run the app
   npm start

```
